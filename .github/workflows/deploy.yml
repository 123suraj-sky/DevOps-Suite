name: CI/CD Pipeline

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-backend:
    name: Build & Test Backend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build & Run Tests
        run: |
          cd backend
          mvn clean test

  build-frontend:
    name: Build & Test Frontend
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Dependencies
        run: |
          cd frontend
          CYPRESS_INSTALL_BINARY=0 npm install --legacy-peer-deps

      - name: Build Frontend
        run: |
          cd frontend
          npm run build

  # ---------------------------------------------------------------------------
  # PUBLISH JOBS
  # Run on both push and pull_request once both test jobs pass.
  # Both images are published in parallel.
  # Docker requires image names to be lowercase, so we derive them from
  # a lowercased github.repository value set in a step.
  # ---------------------------------------------------------------------------

  publish-backend:
    name: Publish Backend Image
    runs-on: ubuntu-latest
    needs: [ build-backend, build-frontend ]

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set lowercase image name
        run: echo "IMAGE=${{ env.REGISTRY }}/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')-backend" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref_name == 'master' }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.IMAGE }}:buildcache
          cache-to:   type=registry,ref=${{ env.IMAGE }}:buildcache,mode=max

      - name: Keep only last 3 successful backend images
        if: steps.push.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          OWNER="${GITHUB_REPOSITORY%%/*}"
          REPO="${GITHUB_REPOSITORY#*/}"
          PACKAGE_NAME="$(echo "${REPO}-backend" | tr '[:upper:]' '[:lower:]')"
          OWNER_TYPE="${{ github.event.repository.owner.type }}"

          if [ "$OWNER_TYPE" = "Organization" ]; then
            API_PATH="/orgs/${OWNER}/packages/container/${PACKAGE_NAME}/versions"
          else
            API_PATH="/users/${OWNER}/packages/container/${PACKAGE_NAME}/versions"
          fi

          echo "Keeping the 3 newest successfully published backend versions..."

          gh api --paginate "$API_PATH?per_page=100" \
            --jq '.[] | [.id, .created_at] | @tsv' |
            sort -k2 -r |
            tail -n +4 |
            cut -f1 |
            while read -r VERSION_ID; do
              echo "Deleting old backend package version: ${VERSION_ID}"
              gh api --method DELETE "${API_PATH}/${VERSION_ID}"
            done

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.IMAGE }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

  publish-frontend:
    name: Publish Frontend Image
    runs-on: ubuntu-latest
    needs: [ build-backend, build-frontend ]

    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set lowercase image name
        run: echo "IMAGE=${{ env.REGISTRY }}/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')-frontend" >> $GITHUB_ENV

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable=${{ github.ref_name == 'master' }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push frontend image
        id: push
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.IMAGE }}:buildcache
          cache-to:   type=registry,ref=${{ env.IMAGE }}:buildcache,mode=max

      - name: Keep only last 3 successful frontend images
        if: steps.push.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          OWNER="${GITHUB_REPOSITORY%%/*}"
          REPO="${GITHUB_REPOSITORY#*/}"
          PACKAGE_NAME="$(echo "${REPO}-frontend" | tr '[:upper:]' '[:lower:]')"
          OWNER_TYPE="${{ github.event.repository.owner.type }}"

          if [ "$OWNER_TYPE" = "Organization" ]; then
            API_PATH="/orgs/${OWNER}/packages/container/${PACKAGE_NAME}/versions"
          else
            API_PATH="/users/${OWNER}/packages/container/${PACKAGE_NAME}/versions"
          fi

          echo "Keeping the 3 newest successfully published frontend versions..."

          gh api --paginate "$API_PATH?per_page=100" \
            --jq '.[] | [.id, .created_at] | @tsv' |
            sort -k2 -r |
            tail -n +4 |
            cut -f1 |
            while read -r VERSION_ID; do
              echo "Deleting old frontend package version: ${VERSION_ID}"
              gh api --method DELETE "${API_PATH}/${VERSION_ID}"
            done

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.IMAGE }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

# ------------------------------------------------------------------------------
# DEPLOY JOB -- COMMENTED OUT
#
# This job was fully implemented and working during the active phase of the
# project. It automatically SSHed into an Azure VM on every push to master,
# pulled the latest code, and rebuilt + restarted the Docker Compose stack.
#
# It has been commented out because the Azure free-tier credits have been
# fully used up and the VM is no longer running. The code is preserved here
# as a reference to show that a complete CI/CD pipeline to Azure was in place.
# ------------------------------------------------------------------------------
#
#  deploy:
#    name: Deploy to Azure VM
#    needs: [build-backend, build-frontend]
#    runs-on: ubuntu-latest
#    if: github.ref == 'refs/heads/master' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
#    steps:
#      - name: SSH Deploy to VM
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.AZURE_VM_IP }}
#          username: ${{ secrets.AZURE_VM_USER }}
#          key: ${{ secrets.AZURE_VM_SSH_KEY }}
#          script: |
#            if [ -d "$HOME/DevOps-Suite" ]; then
#              cd "$HOME/DevOps-Suite"
#            elif [ -d "$HOME/devops-suite" ]; then
#              cd "$HOME/devops-suite"
#            else
#              echo "Repository directory not found!"
#              exit 1
#            fi
#
#            git pull origin master
#            docker system prune -af --volumes=false
#            docker compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache backend
#            docker compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache frontend
#            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d